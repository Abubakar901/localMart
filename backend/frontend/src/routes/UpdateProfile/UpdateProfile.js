import React from 'react';
import { MainContainer, TopContainer, BottomContainer} from './UpdateProfileStyle';

const UpdateProfile = ({ user }) => {
  return (
      <MainContainer>
        <TopContainer>
            <h4>Update Profile</h4>
        </TopContainer>
        <BottomContainer>
            
        </BottomContainer>
      </MainContainer>
  )
}

export default UpdateProfile